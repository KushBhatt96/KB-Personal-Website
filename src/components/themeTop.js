import React from "react"

const ThemeTop = props => {
  return (
    <div className="inner-top">
      <h1 className="text-center page-header">{props.title}</h1>
    </div>
  )
}

export default ThemeTop
